# Test fixtures

Programs the test suites load by name (testing/test_*.pl, editor/tests): not
examples to learn from, and listed on the landing page only to logged-in users
(restricted_paths.pl). The example suite runs them like any example tree;
`testing/test_fixtures_integrity.pl` lists the ones a suite depends on.
