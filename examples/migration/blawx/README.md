# Blawx twins (Phase 2c)

Generated by `lpsPlus/migration/blawx/build.pl` — do not edit by hand; see
`lpsPlus/migration/blawx/README.md` for the mapping, the decisions (the
defeasibility layer written as LE rules) and the results. One directory per
example project of Blawx v1.6.22-alpha (Lexpedite, MIT licence): the LE
program (`<id>.le` — the Act in the words of Blawx's sentence forms, each rule
citing its section, the project's tests and the cases written from its rules
as scenarios, with Blawx's own answers as the expectations, and a View), its
migration ledger (`<id>.ledger.md`, `.json`), and `sources/`: the project file
and the Act's text (`<id>.md`), which the program cites.
