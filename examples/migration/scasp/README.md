# s(CASP) twins (Phase 2c)

Generated by `InsurLE2/migration/scasp/build.pl` — do not edit by hand; see
`InsurLE2/migration/scasp/README.md` for the mapping and the results. One
directory per source program: the LE twin (`<name>.le`, whose scenarios'
expected answers are s(CASP)'s own on the source), its ledger
(`<name>.ledger.md`, `.json`) and the source itself under `sources/`
(File ▸ Show the Original). Thirteen are LE1's own s(CASP) translations (the
sCASP pack's `test/le_programs`), four are classics of its `test/all_programs`.
