% The Logical English twin of an s(CASP) program, written by the s(CASP) translator
% from isdapermissioncorrected-scasp.pl (kept in sources/).
% Every expected answer in the scenarios is s(CASP)'s own answer on the source.
%
% DISCLAIMER. This program was written by a translator from a program of another
% system. It is provided "as is", without warranty of any kind, express or
% implied, including any warranty that it is accurate, complete or fit for a
% particular purpose. The translation may be wrong: check it against its source
% before relying on it. It is not legal, tax, insurance, financial or other
% professional advice. Its authors accept no liability for any loss or damage
% arising from its use.

the target language is: prolog.

the templates are:
    it is permitted that *an eventuality*.
    *an event* of Default occurs with respect to *a party* at *a time*; scenario element.
    *an event* is continuing at *a time*; scenario element.
    *a party* gives notice to *a party* at *a time* that *a message*; scenario element.
    *a date* isonorbefore *a date*; scenario element.
    *a time* isnot more than *a number* days after *a time*; scenario element.
    the Schedule specifies that *a specification*; scenario element.
    Automatic Early Termination applies to *a party* for *an event* of Default; scenario element.
    *an event* occurs at *a time*; scenario element.
    *a party* designates that *an eventuality*; scenario element.

the knowledge base isdapermissioncorrected includes:

the text of the source program is at "sources/isdapermissioncorrected-scasp.pl".

it is permitted that a party designates that Early_Termination_in_respect_of_all_outstanding_Transactions occurs at a time if
    an event of Default occurs with respect to a second party at a second time
    and the event is continuing at a third time
    and the party gives notice to the second party at the third time that the event occurs at the second time
    and the third time isonorbefore the time
    and the time isnot more than 20 days after the third time
    and it is not the case that the Schedule specifies that Automatic Early Termination applies to the second party for the event of Default.

scenario one is, as stated in the source program at scenario one:
    event001 of Default occurs with respect to Bob at Monday.
    event001 is continuing at Tuesday.
    Alice gives notice to Bob at Tuesday that event001 occurs at Monday.
    Tuesday isonorbefore Friday.
    Friday isnot more than 20 days after Tuesday.
    query_1 expects answers ["it is permitted that Alice designates that Early_Termination_in_respect_of_all_outstanding_Transactions occurs at Friday"].

query query_1 is:
    it is permitted that which party designates that which event occurs at which time.

