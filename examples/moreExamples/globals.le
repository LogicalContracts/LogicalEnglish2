the templates are:

the answer is *a policy*.
the other answer is *a policy*.
yet another answer is *a policy*.

our policy is *a policy*;  
    defines global this policy; 
    defines global your policy.   
    % e.g. p(this policy) <-> our policy is X and p(X)

the knowledge base globals includes:

our policy is p123.

the answer is a policy if
    the policy is this policy.

yet another answer is this policy if
    2>1.

the other answer is your policy. % notice this will map internally to a rule


scenario twoPolicies is:
    our policy is p456.
    one expects answers ["the answer is p123", "the answer is p456"].
    two expects answers ["the answer is p123", "the answer is p456"].
    three expects answers ["the answer is p123", "the answer is p456"].

query one is:
    the answer is which policy.
query two is:
    the other answer is which policy.
query three is:
    yet another answer is which policy.


