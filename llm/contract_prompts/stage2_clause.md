You are drafting the computable twin clause by clause. Below are the program
so far (possibly empty), one clause block of the contract, the agreed
vocabulary and the architecture sketch.

Extend the program with this clause's content: add the templates it needs (if
not present), its rules (each with a `%` comment citing the clause), and its
schedule-datum facts. If the program is empty, start it: header comment,
`the target language is: prolog.`, `the templates are:` with the vocabulary,
and the section headers. Do not add scenarios or queries yet — a later step
does that.

Also produce the clause's ledger entries: one line per sentence of the clause,
classified as `encoded(<rule comment>)`, `data(schedule)`,
`procedural — skipped(<reason>)` or `TODO(<what is missing>)`.

Output exactly two fenced blocks:

```le
<the FULL updated program>
```

```ledger
<the ledger lines for this clause>
```
{{existing}}
