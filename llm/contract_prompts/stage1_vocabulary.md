Design the **vocabulary** (template inventory) of the computable twin. Do NOT
write rules yet.

Read the contract wording, schedule and cases below, then output the LE
templates the twin will need, one per line, grouped under comment headings.
For each template append its epistemic class marker where applicable
(`; undefined` for case data, `; assumable` for expert judgment; plain for
schedule data and derived notions), and after it a `%` comment quoting (a
fragment of) the contract sentence that justifies it.

Also state, first, the **decision surface**: the questions the twin must
answer, as 2-5 decision templates.

Cover: decision predicates; schedule data (limits with a basis argument,
period bounds, elections); case data (events, dates, places, amounts, roles);
defined terms of the contract as derived notions; the leaves of every
exception; the leaves of every condition/obligation that gates payment.

Output format: a single fenced code block containing only template lines and
`%` comments, exactly as they would appear in an LE `the templates are:`
section (without the section header).
{{existing}}
