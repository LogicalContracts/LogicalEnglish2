Given the agreed vocabulary and the materials, propose the **architecture** of
the twin: rule skeletons only, not full LE.

Decomposition angle for THIS proposal: {{angle}}

Produce, as markdown:

1. The top-level decision rules (head + bullet list of conditions, using only
   vocabulary templates or derived notions you name here).
2. The derived notions (each: head template + one-line meaning).
3. The exception rules (one per contract exception, each with its clause
   anchor).
4. The parameter/limit machinery (which generic rules apply the schedule's
   limit facts).
5. Which case facts the scenarios will need, per case.

Stay within the vocabulary; if a skeleton needs a template that is missing,
list it explicitly under "Additional templates required".
{{existing}}
{{instructions}}
