Translate the residue blocks of the skeleton below. The residue ids are:

{{ids}}

Reply with ONE fenced block per residue, its info line naming it, and nothing
else of substance:

```le residue r1
<the Logical English rules for residue r1>
```

and, only if you need a template the skeleton does not declare:

```le residue templates
<one template per line>
```

Each block holds knowledge-base statements only (rules and facts) — no section
headers, no scenarios, no queries. It is spliced between the residue's markers
as it is.
{{instructions}}
